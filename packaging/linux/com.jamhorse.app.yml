app-id: com.jamhorse.app
runtime: org.freedesktop.Platform
runtime-version: '24.08'
sdk: org.freedesktop.Sdk
command: jamhorse
finish-args:
  - --share=network
  - --socket=pulseaudio
  - --socket=wayland
  - --socket=fallback-x11
  - --talk-name=org.freedesktop.secrets
modules:
  - name: jamhorse
    buildsystem: simple
    build-commands:
      - install -Dm755 jamhorse /app/bin/jamhorse
      - cp -a lib /app/bin/lib
      - cp -a data /app/bin/data
      - install -Dm644 com.jamhorse.app.desktop /app/share/applications/com.jamhorse.app.desktop
      - install -Dm644 com.jamhorse.app.metainfo.xml /app/share/metainfo/com.jamhorse.app.metainfo.xml
      - install -Dm644 jamhorse-512.png /app/share/icons/hicolor/512x512/apps/com.jamhorse.app.png
      - install -Dm644 LICENSE /app/share/licenses/jamhorse/LICENSE
      - install -Dm644 THIRD_PARTY_NOTICES.md /app/share/licenses/jamhorse/THIRD_PARTY_NOTICES.md
    sources:
      - type: dir
        path: ../../build/linux/x64/release/bundle
      - type: file
        path: com.jamhorse.app.desktop
      - type: file
        path: com.jamhorse.app.metainfo.xml
      - type: file
        path: ../../assets/icons-output/web/icon-512.png
        dest-filename: jamhorse-512.png
      - type: file
        path: ../../LICENSE
      - type: file
        path: ../../THIRD_PARTY_NOTICES.md
